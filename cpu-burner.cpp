#include <iostream>

int main() {
    std::cout << "CPU burner is running and will keep the CPU busy..." << std::endl;

    while (true) {
        // Do nothing, just keep looping to use up CPU cycles
    }

    return 0;
}
